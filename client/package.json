{
  "name": "chores",
  "version": "0.1.0",
  "description": "Front-end for Chores app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "rm -r build && mkdir -p build",
    "build": "browserify -t [ babelify ] src/index.jsx -o build/bundle.js"
  },
  "author": "Joshua Ochakovsky",
  "license": "GPL-3.0",
  "dependencies": {
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-react": "^6.5.0",
    "babelify": "^7.3.0",
    "browserify": "^13.0.1",
    "react": "^15.1.0",
    "react-dom": "^15.1.0"
  }
}
